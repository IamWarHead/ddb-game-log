<div class="ddb-game-log-chat-card">
    <hr/>
    <header class="ddb-card-header flexrow">
        <img src="{{iconUrl}}" title="Shortbow">
        <h3 class="ddb-item-name" style="">{{action}}</h3>
    </header>
    <div class="ddb-card-content">
        {{description}}
        <a onclick="{{{opener}}}" data-ddb-entry-id="{{entryId}}" data-ddb-type="{{type}}"><i class="fas fa-link"></i> Open compendium entry</a>
        <a href="{{ddbUrl}}"><i class="fas fa-external-link-alt"></i> Open on D&amp;D Beyond</a>    
    </div>
</div>